# Juno Phase 3 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use subagent-driven-development for implementation.

**Goal:** Implement Phase 3 features: AI外教对话 (Proactive Probing) and 实景视觉锚定 (Visual Reality Anchoring).

**Architecture:** Clean Architecture with MVVM. UI Layer → Domain Layer → Data Layer.

**Tech Stack:**
- UI: Jetpack Compose + Material 3
- Camera: CameraX
- Image Recognition: ML Kit Image Labeling
- DI: Hilt
- Backend: LLM integration (placeholder)

---

## Phase 3 Overview

### Core Feature 5: AI追问式多外教 (Proactive Probing)
1. **AI外教对话系统** - 主动提问式对话
2. **多外教模式** - 不同性格/口音的AI Tutor
3. **对话上下文管理** - 基于用户学习的单词

### Core Feature 6: 实景视觉锚定 (Visual Reality Anchoring)
1. **相机拍照** - CameraX集成
2. **物体识别** - ML Kit Image Labeling
3. **生成专属内容** - 为用户物品生成英语知识

---

## Task 1: Dependencies

### Step 1.1: Add CameraX dependencies

```kotlin
// build.gradle.kts
implementation("androidx.camera:camera-core:1.3.1")
implementation("androidx.camera:camera-camera2:1.3.1")
implementation("androidx.camera:camera-lifecycle:1.3.1")
implementation("androidx.camera:camera-view:1.3.1")

// ML Kit Image Labeling
implementation("com.google.mlkit:image-labeling:17.0.8")
```

---

## Task 2: AI外教对话系统

### Step 2.1: Create AI Tutor Models

```kotlin
// data/remote/AiTutorService.kt
data class TutorProfile(
    val id: String,
    val name: String,
    val personality: String,
    val avatar: String,
    val description: String
)

data class ChatMessage(
    val id: String,
    val content: String,
    val isFromUser: Boolean,
    val timestamp: Long
)
```

### Step 2.2: Create AI Chat Service

```kotlin
// data/remote/AiChatService.kt
class AiChatService {
    // 预设外教角色
    val tutors = listOf(
        TutorProfile("strict", "Professor Smith", "严谨的大学讲师", ...),
        TutorProfile("friendly", "Traveler Mike", "热情的旅行博主", ...),
        TutorProfile("funny", "Comedian Emma", "幽默的英语老师", ...)
    )
    
    // 根据用户学习的单词生成对话
    suspend fun generateProactiveQuestion(
        tutorId: String,
        learnedWords: List<String>,
        userLevel: String
    ): String
    
    // 处理用户回复并给出反馈
    suspend fun processUserResponse(
        message: String,
        context: ChatContext
    ): String
}
```

### Step 2.3: Create Chat Screen

```kotlin
// ui/screens/chat/ChatScreen.kt
@Composable
fun ChatScreen(
    tutorId: String,
    onNavigateBack: () -> Unit
) {
    // 对话列表
    // 输入框
    // 外教选择器
}
```

---

## Task 3: 多外教模式

### Step 3.1: Create Tutor Selection Screen

```kotlin
// ui/screens/tutor/TutorSelectionScreen.kt
@Composable
fun TutorSelectionScreen(
    onSelectTutor: (String) -> Unit
) {
    // 显示可用的外教列表
    // 每个外教显示：头像、名字、性格描述
}
```

### Step 3.2: Create Chat ViewModel

```kotlin
// ui/screens/chat/ChatViewModel.kt
@HiltViewModel
class ChatViewModel @Inject constructor(
    private val aiChatService: AiChatService
) : ViewModel() {
    // 管理对话状态
    // 发送消息
    // 接收AI回复
}
```

---

## Task 4: 实景视觉锚定

### Step 4.1: Create Camera Service

```kotlin
// data/local/CameraManager.kt
class CameraManager {
    // CameraX初始化
    // 拍照功能
    // 预览
}
```

### Step 4.2: Create Image Recognition Service

```kotlin
// data/remote/ImageRecognitionService.kt
class ImageRecognitionService {
    // 使用ML Kit识别图中物体
    suspend fun recognizeObjects(bitmap: Bitmap): List<RecognizedObject>
}

data class RecognizedLabel(
    val label: String,
    val confidence: Float
)
```

### Step 4.3: Create Visual Anchor Service

```kotlin
// data/remote/VisualAnchorService.kt
class VisualAnchorService {
    // 根据识别的物体生成英语知识
    suspend fun generateAnchorContent(
        labels: List<RecognizedLabel>
    ): List<AnchorContent>
}

data class AnchorContent(
    val word: String,
    val meaning: String,
    val funFact: String,
    val pronunciation: String
)
```

### Step 4.4: Create Camera Screen

```kotlin
// ui/screens/camera/CameraScreen.kt
@Composable
fun CameraScreen(
    onCapture: () -> Unit,
    onNavigateBack: () -> Unit
) {
    // CameraX预览
    // 拍照按钮
    // 识别结果展示
}
```

### Step 4.5: Create Anchor Result Screen

```kotlin
// ui/screens/anchor/AnchorResultScreen.kt
@Composable
fun AnchorResultScreen(
    recognizedLabels: List<RecognizedLabel>,
    anchorContents: List<AnchorContent>
) {
    // 显示识别的物体
    // 显示生成的英语知识卡片
    // 可以添加到单词本
}
```

---

## Task 5: Integration

### Step 5.1: Update Navigation

```kotlin
// Screen.kt
object TutorSelection : Screen("tutor_selection")
object Chat : Screen("chat/{tutorId}")
object Camera : Screen("camera")
object AnchorResult : Screen("anchor_result")
```

### Step 5.2: Add to Home Screen

在首页添加：
- "AI对话" 入口
- "拍照学英语" 入口

---

## Summary

### What Will Be Built

1. **AI外教对话系统**
   - TutorSelectionScreen - 选择外教
   - ChatScreen - 对话界面
   - AI Chat Service - 对话逻辑

2. **实景视觉锚定**
   - CameraScreen - 拍照
   - ImageRecognitionService - 物体识别
   - VisualAnchorService - 生成英语知识
   - AnchorResultScreen - 结果展示

---

## Execution

Ready to implement? I will use subagents to implement each feature in parallel.
